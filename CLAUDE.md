# Claude Code — Project Context

See [AGENT_CONTEXT.md](AGENT_CONTEXT.md) for full architecture, coding rules, and guidelines.
See [FEATURES.md](FEATURES.md) for epic/feature map and code locations.
See [BACKLOG.md](BACKLOG.md) for current tasks.

## Important: Always use FVM
Run all Flutter commands with `fvm` prefix (e.g. `fvm flutter analyze`, `fvm flutter test`).
Bare `flutter` points to the wrong SDK version.
